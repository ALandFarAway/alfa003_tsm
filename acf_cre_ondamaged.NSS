////////////////////////////////////////////////////////////////////////////////
//
//  System Name : ACR Configuration File
//     Filename : acf_cre_ondamaged
//    $Revision:: 280        $ current version of the file
//        $Date:: 2007-03-20#$ date the file was created or modified
//       Author : Cipher
//
//    Var Prefix:
//  Dependencies:
//
//  Description
//  This script calls the ACR's OnDamaged event handler for creatures and
//  any custom code a server may need. It is not updated in ACR updates.
//
//  Revision History
////////////////////////////////////////////////////////////////////////////////

////////////////////////////////////////////////////////////////////////////////
// Includes ////////////////////////////////////////////////////////////////////
////////////////////////////////////////////////////////////////////////////////
#include "acr_creature_i"

////////////////////////////////////////////////////////////////////////////////
// Constants ///////////////////////////////////////////////////////////////////
////////////////////////////////////////////////////////////////////////////////

////////////////////////////////////////////////////////////////////////////////
// Structures //////////////////////////////////////////////////////////////////
////////////////////////////////////////////////////////////////////////////////

////////////////////////////////////////////////////////////////////////////////
// Global Variables ////////////////////////////////////////////////////////////
////////////////////////////////////////////////////////////////////////////////

////////////////////////////////////////////////////////////////////////////////
// Function Prototypes /////////////////////////////////////////////////////////
////////////////////////////////////////////////////////////////////////////////

////////////////////////////////////////////////////////////////////////////////
// Function Definitions ////////////////////////////////////////////////////////
////////////////////////////////////////////////////////////////////////////////

void main()
{
    ACR_CreatureOnDamaged();

    // Custom code goes here.
	object oDamager = GetLastDamager();
	object oWeapon = GetLastWeaponUsed(oDamager);
	if(GetLastWeaponUsed(oDamager) != OBJECT_INVALID) 	
		{SendMessageToAllDMs(GetName(oDamager) + " damaged " + GetName(OBJECT_SELF) + " with " + GetTag(oWeapon));}
	else if((GetLastSpellCaster() == oDamager) && (GetLastSpellCastClass() != CLASS_TYPE_INVALID))
		{SendMessageToAllDMs(GetName(oDamager) + " damaged " + GetName(OBJECT_SELF) + " with " + IntToString(GetLastSpell()));}
	else {SendMessageToAllDMs(GetName(oDamager) + " damaged " + GetName(OBJECT_SELF) + " with something else.");} 
	
	
	if(GetLocalInt(OBJECT_SELF, "Proxy") == 1) {
	    SetLocalInt(OBJECT_SELF, "Proxy", 2);	
		int nAppearance = GetLocalInt(OBJECT_SELF, "Appearance");     
        effect eRise = EffectVisualEffect(VFX_FNF_SUMMON_EPIC_UNDEAD, FALSE);
		ApplyEffectToObject(DURATION_TYPE_TEMPORARY, eRise, OBJECT_SELF, 5.0);
		DelayCommand(1.0, SetCreatureAppearanceType(OBJECT_SELF,nAppearance));
	    object oMyTrigger = GetLocalObject(OBJECT_SELF,"Undead_Proxy_Trg");
        string sMessage = GetLocalString(oMyTrigger, "ACR_SPAWN_MESSAGE");
		DelayCommand(1.5, ActionSpeakString(sMessage, TALKVOLUME_TALK));
		effect eEffect = GetFirstEffect(OBJECT_SELF);
		while(GetIsEffectValid(eEffect)) {
			RemoveEffect(OBJECT_SELF, eEffect);
			eEffect = GetNextEffect(OBJECT_SELF);
			}
		object oAttack = GetNearestCreature(CREATURE_TYPE_PLAYER_CHAR, PLAYER_CHAR_IS_PC, OBJECT_SELF, 1);
		ActionAttack(oAttack, FALSE);
		}
		
}